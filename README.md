# ok-nte-launcher
该脚本用于配合[ok-nte](https://github.com/BnanZ0/ok-nte/)使用，用于打开异环启动器，并且启动异环

安装依赖
```
pip install -r requirements.txt
```

完整运行脚本`python main.py`
首次运行时，需要先打开异环启动器，
并且修改main.py中的OKNTE_PATH为你的ok-nte.exe路径

单独运行异环启动器脚本`python launcher.py -t 1`